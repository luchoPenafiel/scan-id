// Vendors
import React from 'react';

// Styles
import { Wrapper } from './Header.styles';

export const Header = () => {
  return (
    <Wrapper data-testid="header">
      <h1>BankClient</h1>
    </Wrapper>
  );
};
